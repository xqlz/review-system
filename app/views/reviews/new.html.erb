<%= form_for @review do |f| %>
	<p>
		<%= f.label :rating %>
		<%= f.text_field :rating %>
	</p>
	<p>
		<%= f.label :comment %>
		<%= f.text_field :comment %>
	</p>

	<%= f.submit 'Submit' %>
<% end %>